tasks:
  - task: install_code
    arguments:
      - vscode-updates/argv.json
  - task: install_code_extensions
    arguments:
      - codeExtensions
  - task: configure_code
    arguments:
      - vscode-updates/user-settings.json
    enabled: true
