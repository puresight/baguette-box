tasks:
  - task: install_code
    arguments:
      - code/argv.json

  - task: install_code_extensions
    arguments:
      - code/code.dep

  - task: configure_code
    arguments:
      - code/user-settings.json
    enabled: true
