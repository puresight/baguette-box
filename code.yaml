tasks:
  - task: install_code
    arguments:
      - code-update/argv.json

  - task: install_code_extensions
    arguments:
      - Codefile

  - task: configure_code
    arguments:
      - code-update/user-settings.json
    enabled: true
