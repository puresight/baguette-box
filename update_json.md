# Update Json

This script is a bash **function** used to perform a shallow merge that updates a VS Code settings file with keys/values from a file in this repository.

## Side effect

Unfortunately, the cost of this functionality is loss of JSON comments in the files.
