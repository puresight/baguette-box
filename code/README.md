# Code

This folder holds all information for installing and configuring VS Code,
including the `code.dep` list of extension dependencies to install.

Here you will find JSON files with configuration updates
that shall be applied (in a shallow merge)
when the Code IDE is installed or configured, including

- argv.json
- user-settings.json
